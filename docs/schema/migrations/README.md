# Schema Migration Manifests

Migration manifests live under:

```text
docs/schema/migrations/<domain>/<kind>/v<from>-to-v<to>.md
```

See [`../schema-versioning.md`](../schema-versioning.md) for the required
manifest shape and compatibility policy.
